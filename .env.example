MONGODB_URI=mongodb://localhost:27017/ecofood
JWT_SECRET=change_this_secret
PORT=4000

# GROK API key for the new AI provider (replace with your real key)
GROK_API_KEY=#place your api key here
# Optional: override the Grok base URL if needed
# GROK_API_URL=https://api.grok.ai/v1

# Email Configuration (for password reset, notifications, etc.)
# For Gmail, you MUST use an App Password (not your regular password)
# Steps to create Gmail App Password:
# 1. Enable 2-Factor Authentication on your Google account
# 2. Go to: https://myaccount.google.com/apppasswords
# 3. Select "Mail" and your device
# 4. Copy the 16-character password and paste below
# If not configured, OTPs will be logged to console for development
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password
